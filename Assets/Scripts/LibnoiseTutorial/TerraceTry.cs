using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using LibNoise;
using LibNoise.Generator;
using LibNoise.Operator;

public class TerraceTry : MonoBehaviour
{
    //void Generate()
    //{
    //    var perlin = new Perlin();

    //    var scaled = new ScaleBias(Scaling, ScaleBias, perlin);
    //    var final = new Terrace(false, scaled);

    //    //for (float  i = 0; i < 50; i += 10)
    //    final.Add(0f);
    //    final.Add(10f);
    //    final.Add(100f);

    //    var noise = new LibNoise.Unity.Noise2D(TotalSize, final);
    //}
}
